---
name: prop-asset
description: >
  道具资产参考图生成预设。触发词：道具、物品、物件、道具设计、道具资产、prop、物品图。输出静态参考图任务说明与资产合同，
  本 Skill 独立完成静态参考图任务说明、资产锁定、审核字段、负向约束和可供后续制作直接读取的资产合同。
---
## 青木适配

素材设计先用原生 skill 工具读取 `production-design` 的共用美术设计与提示词方法，再用本包细则落实当前素材；查询 ID 或生成进度无需加载。

本包作为 Leos 六部门的资产设计专项资料。当前作品、导演决定与用户授权优先；用户已委托完整创作时，缺失设计由导演提出并采用，不逐项打断询问。涉及现有真实人物的身份资料只从当前项目读取。案例不是项目事实，数值不是永久限制，多视图与十段式按需要选取。仅用青木真实工具读写与生成；审核结果如实标注操作者，不把代理审看冒充用户亲自认可。技能文件本身不授予费用或状态写入权限。


# 道具资产参考图生成预设

> 当前包已经包含全部运行规则、参数依据和质量门；不使用未经用户确认的人脸或生物特征。

## 0. 加载规则与映射表

确认资产设计任务 → 选择交付与成像模式 → 读本包画面关系判断 → 提取第1节字段 → 按第2节一致性锁定 → 第3–5节补足材质/光影/几何 → 选择平台和第7节适用模板 → 按当前交付阶段执行第8节检查与第9节负向 → 输出资产合同。资产编号：P01–P99，已确认编号不得复用。

| 节 | 独立输出 | 用途 |
|---|---|---|
| 1–5 | 资产描述、锁定规则 | 确定外观与状态 |
| 6–7 | 平台任务与提示词字段 | 直接生成参考图 |
| 8–9 | 验收和防崩 | 独立质量检查 |
| 10 | JSON 合同 | 供后续制作直接读取 |
| 11 | 类型适配字段 | 注入成片提示词的对应段落 |

### 交付与成像模式

按用户点名的结果选择，不自动扩展为整套图：

- **提示词/设计说明**：交付当前请求的完整提示词、必要锁定信息与资产合同；尚未生成的参考图用空列表并标为计划，不填写虚构图片路径或声称图片通过。
- **实际参考图**：用户要求出图且有可用能力时生成所需图片；单图先交单图，整套资产请求或明确跨视角验收需求才扩展角度。实际看图后记录检查结果，用户批准前保持 pending_review。
- **诊断/局部修改**：围绕给定图或提示词指出可见问题、原因假设和对应修正；保留未点名的身份、风格与状态，不默认重做整套资产。

先锁定摄影写实、三维动画、二维动画/插画或其他用户成像方式，再按本包 [电影画面关系判断](references/cinematic-image-direction.md) 设计画面关系与负向。此参考在设计、改写、诊断时读取；编号/文件查询不必加载。模板中的多格、人物尺度、标准布光和材质纹理均按用途选取，不因模板固定成套、写实或宏大。

## 1. 描述维度表

必填维度：类型、用途、尺寸、材质、颜色、重量感、状态/磨损、A面/B面、可读文字约束、初始持有人、朝向。

规则：用户未给出的关键维度标为 待确认，不臆造；已确认维度写入资产合同的 locked_features；描述按从整体到局部、从外观到材质、从静态到状态的顺序组织。

### 1.1 年代、构造与物面文字

先按用途、时代地域和剧本明确例外确定器物形制，再描述材料表面。必须能完成剧情需要的动作：书册写清装订边与自由翻页边，容器写清开合连接，穿戴件写清固定方式；不要把缝线、布套、绑带等相关词随意堆在同一物件上。材料的新旧由使用史决定，古代不等于破烂，现代商品也不继承旁边古物的做旧。单件主体与附属容纳物分开，跨镜状态与固定形制分开。

需要绘制的字串逐字引号标示，说明书写或印制方式及具体位置；版式解释、字段名、工作指令不作为可见文字。参考图中的错误结构或字样不能继续当作必须保留的外观。先确认本轮保留的身份依据与要改的可见偏差，再采用支持目标的参考；图片返回后检查整个物件，不以局部修正代替整体验收。

### 共用尺度与手持关系

同一 AssetID 的实体尺寸作为稳定锚点：明确长宽厚/直径、统一单位、含盖或展开等测量状态、依据与确定性。产品净重不是外形尺寸，商品外箱尺寸不是管体尺寸，参考图的像素占比不是绝对长度。没有实测或厂家资料时，由导演采用合理拍摄估值并明确标注，不能冒充来源事实；后续镜头沿用同一估值，有新来源再统一更新。

手持道具写出与手掌、前臂或人物身高的相对关系；装盒、抽取或放桌时核对容纳关系与接触面。特写增加画面占比、浅景深改变焦点，均不意味着物体变大。稳定尺寸写入 visualIdentity，具体镜头引用时写进可见描述，临时开合/压扁状态写 imageStage。实际出图同时查看整体比例和接触，不以提示词有数字代替画面达标。

## 2. 一致性/状态规则

可移动道具的默认身份参考是单件主体、单一明确状态、纯色无缝背景。保留物体自身的材质、边缘和自然接触阴影；不加入窗户、墙面、家具、人物、手或其他器物辅助尺度，也不把名称中的持有人画成主体。剑与剑鞘等确属同组的部件可以同图，逐件说明数量和分开、插入或连接关系，不能额外摆出多个状态的复制品。

按实际需要选择 A面、B面、侧面或关键细节。尺寸及其依据保存在主体设定；只有本次确需验证握持、安装或使用时才另设计交互图，并在用途注明交互与所在场景，不能拿它替代干净身份图。固定建筑构件按其实际安装关系设计，不强行当作可移动小物。锁定材质、新旧状态、可见面和扣件/开口位置。

变体规则：服装、受伤、昼夜、损坏等变化使用原编号后缀（如 P01b）并记录变化原因；不得悄然覆盖已审核版本。参考图一经通过资产审门，后续视频只可引用，不能重新发明外观。

## 3. 材质细化库

纸张纤维/折痕/卷边、金属拉丝/氧化、木材纹理、玻璃厚度/反射、布料织纹、皮革毛孔、塑料注塑线。

粗词禁止：高级材质、精致金属、真实皮肤、电影级纹理。每项必须改写为表面结构、磨损、反射、接触或微动态。材质与光源、接触点、重力方向一致。

## 4. 光影规则

柔主光、轮廓光、背景分离；反射材质保留真实高光而非镜面泛白。

标准资产图优先可读性而非情绪滤镜：主体与背景分离、关键细节不被死黑吞没、反射不遮挡结构；风格色彩作为可切换层，不能改变资产真实颜色记录。

## 5. 比例、透视与物理规则

手指与道具接触要有受力、遮挡、阴影和有限变形；重量、开合、液体、方向均符合物理。

禁止：结构断裂、无承重关系、穿模、悬浮、镜像/重复零件、透视与光影方向相互冲突。本次任务确需交互校验时，才另设计使用状态图；不因后续剧情有动作而自动增加图片。

## 6. 平台适配

| 平台 | 输出重点 | 保留字段 |
|---|---|---|
| Midjourney | 英文主体、视角、镜头、材质、背景隔离 | aspect ratio / reference / version |
| 即梦 | 中文分段：主体—细节—状态—背景—光影—约束 | 资产编号与禁止项 |
| 可灵 | 图生视频前的多角度、材质、动作锚 | reference image / 状态起点 |
| Stable Diffusion | 英文正向/负向与可复现参数 | seed / model / reference / negative |

任何平台都不得改变 locked_features；平台版本能力未知时标记 unverified，不作为硬约束。

## 7. 资产提示词模板

适用模板：照片、武器、信件、设备、容器、食物、钥匙。

~~~
[prop资产编号]，[主体和用途]，[全维度描述]，
[正面/侧面/背面/局部或空间视角]，[真实材质和磨损]，
[标准布光和背景分离]，[比例/透视/物理规则]，
[目标平台参数]，[三层负向]。
~~~

按已选模式交付提示词、实际参考图或诊断/局部修改及相应资产合同；多视角/状态图仅在本次用途需要时生成。下列多格与手持写法仅用于明确需要的整套或交互校验任务；普通身份参考只取单件、一个指定视角和纯色无缝背景，不拼版；不把视频动作或台词写入静态资产提示词。

### 模板A：照片/信件

~~~text
[Pxx名称]，[用途/尺寸/纸张类型/颜色/磨损]，[A面内容限制/B面内容或空白]，[折痕、卷边、污渍]，
A面+B面+侧面+手持比例图，柔主光+轮廓光+背景分离，纸纤维和受力弯曲可见，
锁定可见面、朝向、初始持有人和最终状态；不生成可读乱码文字。
~~~

### 模板B：武器/工具

~~~text
[Pxx名称]，[类别/用途/尺寸/重量感]，[金属/木材/皮革/涂层]，[磨损、接口、安全结构]，
左右侧面+细节+手持比例图，光源可见、边缘反射真实，锁定开关、开口、握持方向，
接触和承重可信，禁止漂浮、枪口/刃口方向错误、结构增殖。
~~~

### 模板C：设备

~~~text
[Pxx名称]，[功能/尺寸/材质/颜色]，[正面面板、背面接口、侧面厚度]，[磨损与状态灯]，
A面+B面+侧面+手持比例图，柔光控制反射，锁定按钮/接口/屏幕位置，
屏幕不生成乱码或无意义文字；电缆、散热、开合和重力方向一致。
~~~

### 模板D：容器/食物

~~~text
[Pxx名称]，[内容物/容积/材料/颜色]，[开口、封口、液位或食物状态]，[磨损/污渍]，
正面+背面+俯视+手持比例图，玻璃厚度/金属反射/液体重力真实，
锁定开合状态与朝向，禁止无故溢出、穿模、重复容器和不合逻辑反射。
~~~

### 模板E：钥匙/小物

~~~text
[Pxx名称]，[用途/尺寸/材料/颜色]，[齿纹/孔洞/刻痕/挂件]，[磨损状态]，
正反面+侧面+手指比例图，微距材质可读、边缘受光真实，
锁定 A/B 面、孔位、朝向和持有人；禁止变成多把、漂浮、文字乱码或过亮发光。
~~~
## 8. 质量检查清单

- [ ] 编号、版本、名称和用途唯一
- [ ] 第1节必填维度齐全或明确待确认
- [ ] 锁定特征跨视角一致
- [ ] 主体比例/透视/结构正确
- [ ] 材质可见且符合光源
- [ ] 接触、重量和阴影可信
- [ ] 关键面、朝向、开口/接口可辨
- [ ] 无多余肢体、物件、文字或水印
- [ ] 可移动道具身份图只有所需主体与纯色无缝背景，无人物、家具、门窗或其他器物；使用状态图用途单独标明
- [ ] JSON 合同可被下游解析

按交付阶段检查：提示词只验证设计与合同，未生成/未见图片的像素项目标 not_evaluated；实际图片的适用项目失败时为 rework。只有实际参考图通过用户审阅才进入 assets_approved，文本通过不替代看图。

## 9. 负向提示词

第一层（通用）：畸形、扭曲、多余肢体、错误手指、穿模、粘连、漂浮、重复元素、乱码、水印、低清、闪烁。
第二层（prop）：穿模、无依据漂浮或数量变化、方向错误、与锁定不符的新旧/磨损、非设计发光、错误文字、尺寸漂移。新出厂物品保留新表面，旧物按使用史分配磨损；灯具、状态灯和能量道具保留有依据的发光与对应照明，真实塑料保留其粗糙度和注塑特征。\
第三层（画质）：先按用户成像方式选择。摄影写实排除不符合目标的卡通化、蜡皮、合成渲染感；三维动画、二维动画/插画保留选定风格，排除结构错误、风格漂移或意外写实化。各模式按需要限制低清、遮蔽结构的过曝/欠曝、过度锐化；不一律禁止动画、卡通或真实塑料材质。

十条通用反面案例与三层负向写法详见 `references/NEGATIVE-CASE-BOOK.md`；本节的专属负向与之叠加，不可互相替代。

## 10. 注入接口

~~~
{
  "asset_type": "prop",
  "asset_id": "P01",
  "version": "v1.0",
  "status": "pending_review",
  "reference_images": [],
  "locked_features": {},
  "material_keywords": [],
  "state_contract": {"initial":"","change_nodes":[],"final":""},
  "source_skill": "prop-asset",
  "quality_check": "pending"
}
~~~

本 Skill 自己完成资产审核字段、编号登记、参考输入说明和对照复核要求；后续制作只读取本 Skill 已交付的资产合同，不得重写锁定特征。

## 11. 类型适配字段

将道具参考图和状态账直接写入视频第③/⑥段或等价的动作与材质字段。

流程：资产生成 → 人审 assets_approved → 本地类型适配 → 10段式拼装 → 每镜引用资产编号 → 当前 Skill 的QC字段对照资产原图、实际尾帧和状态合同。若未获人审，不得进入正式镜头制作。
