# 正式交付模式门

## 用途

本文件只决定普通请求应交付哪一层成品，并检查提示词正文是否真正完整。它不修改当前版本 `production-contract.md` 的空间、物理、连续性和六模块合同。

## 默认判定

- 普通“拆分镜、做分镜、写视频提示词、继续分镜”请求，默认同轮交付人读五列分镜与完整六大模块视频提示词。
- 只有用户明确要求“先看分镜、只要分镜表、暂不写提示词、先确认镜头”时，才停在人读分镜层。
- 用户点名“提示词、完整提示词、可复制提示词”时，第一交付层必须包含写满具体内容的六大模块正文；不得用分镜表、分析、短镜头句、六个空标题、映射表或结构检查代替。
- 用户点名一个总时长时，默认交付一条该总时长的母提示词，六大模块只出现一次；不得把内部资产或镜头分段自动扩成多条重复结构。
- 用户可见提示词只写角色、地点、物件的自然名称，不写内部编号、文件名、路径、版本、哈希、审批状态或项目字段。
- 有生命的主体必须具有来自剧本、用户要求、物种、性格和当下目的的连续微动作，但不得为了丰富画面擅自加戏。
- 用户点名提示词但尚未逐镜确认时，依据现有剧本完成一个可执行方案并交付；只有关键歧义会改变结果时才追问一个问题。

## 完整可复制判定

每个模块必须是针对当前片段的具体正文，不是字段名、占位符或摘要：

- “一、生成任务”写清时长、画幅、帧率和整段生成任务；
- “二、主要主体”写清主体身份、外观、初始动作状态和不可改变特征；
- “三、场景状态”建立一次固定世界地图，并锁定道具、实体光源和环境动力；
- “四、情绪目标”把情绪写成表演、关系和局面变化，不只写抽象形容词；
- “五、视觉风格”写清摄影、光色、材质、运动质感和电影风格；
- “六、分段脚本”按非等分时间写完整动作与镜头过程，包含声音/台词、物理反馈、最终可见状态，并以全段硬约束收尾。
- “六、分段脚本”的每个时间段标题直接显示具体景别/焦段/光学、机位方位+高度+俯仰、摄影机中心路径+朝向变化、速度质感、焦点/遮挡接力和镜头落点；剧情短标题不得代替这些摄影信息。
- 固定机位、锁定焦点和复合运镜的分阶段路径/速度变化必须显式写出，不留空项，也不用“电影感运镜”“复合调度”等模糊词代替。

数字10十项信息只能注入上述六模块。不得增加“七、八、九、十”等同级外显章节。

## 行为验收

不能用标题或字段计数判断通过。必须读取实际产物并确认：

1. 用户剧本中的人物、道具、台词和动作结果都在相应时间段得到具体处理；
2. 每个镜头的摄影机位置、构图、动作因果和终点状态可被实际执行；
3. 台词与声音出现在可容纳它们的时间段，没有被摘要、删改或吞词；
4. 场景世界状态、光源和环境动力在换机位后仍连续；
5. 第一交付层确实包含分镜表与完整提示词正文，而不是对格式的说明。
6. 用户要求一条总时长提示词时，六大模块是否只出现一次并覆盖完整时长；拆分生产单元是否只在明确要求或已核实硬限制下后置。
7. 提示词正文中的人物、场景和道具是否全部使用自然名称，内部编号及治理字段命中为0。
8. 有生命的角色是否具有连续自然动作，同时没有新增原剧本之外的事件、角色、地点、道具或对白。
9. 每个时间段标题是否真正让用户看到“这一镜怎么拍”，包含具体焦段/光学、机位、路径、朝向变化、速度、焦点和落点，没有只给“水与手”一类剧情标题。
10. 只读最终提示词能否还原已选择的摄影方案，尤其关键细节的取景距离、特殊角度、复合路径各阶段、人物/焦点/遮挡接力和结果停留；不能只按术语或标题计数。
11. 关键动作在镜头中既有可见尺度也有观看时间；动作、摄影和焦点变化没有在同一短时间窗互相争夺注意，固定构图也有明确取景和切点。
