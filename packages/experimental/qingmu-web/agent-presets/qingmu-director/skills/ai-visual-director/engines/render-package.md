# 渲染打包引擎

最终一键输出「可执行视频生成包」。Prompt 已在 video-prompt-assembly → consistency-engine → prompt-scorer → auto-repair 完成，本引擎负责打包全部资产+平台参数+执行清单。

---

## 输出包结构

```markdown
【视频生成执行包】

项目名称：[片名]
目标平台：[平台名]
时长/镜数/画幅：[Ns]/[N镜]/[比例]
═══════════════════════════════

📦 资产清单（按生成顺序）
  1. [类型]：[文件名/prompt描述] — 用途：[锁什么]
  2. ...

📋 分镜图生成Prompt
  [完整分镜图prompt]

🎬 视频Prompt
  [来自 video-prompt-assembly，已通过 prompt-scorer + auto-repair + final-video-qc 校验]

⚠ 平台参数
  平台：[名称]
  字数：[当前/上限]
  参考图：[已附N张/上限M张]
  语言：[中文/英文]
  特殊参数：[--cref / first_frame / etc.]

🚫 负面提示词
  [由 video-prompt-assembly 生成，基于 rules/negative-prompt.md + rules/quality.md 的 AI 错误库]

✅ 最终检查清单
  □ 角色DNA已锁定[角色名]
  □ 场景锚点已声明
  □ 首帧=镜1起点
  □ 尾帧=最后镜终点
  □ 运动方向逐镜声明
  □ Prompt字数在平台限制内
  □ 中文/英文已按要求
  □ 参考图数量在平台限制内

🔄 跨段衔接（如拆段）
  段1尾帧 → 段2首帧：
    尾帧状态：[角色位置] / [动作末态] / [视线方向] / [光线方向] / [画面调性] / [道具状态] / [情绪状态] / [空间关系]
    首帧要求：[继承尾帧全部 8 字段]
    衔接技法：[技法编号]
```

---

## 单段 vs 多段

### 单段输出

如果 `shot-budget` 判断"一次生成"，直接输出上述完整包。

### 多段输出

如果 `shot-budget` 判断"需拆段"：

```markdown
═══════════════════════════════
📦 段1/3：[段描述]
  时长：[Ns] / 镜数：[N镜]
  资产：[列表]
  视频Prompt：[prompt]
  ────────────────────────────
  段1尾帧：[状态快照，8字段 end_state]
    ↓ 衔接技法：[编号]
  段2首帧：[继承段1尾帧全部 8 字段]
═══════════════════════════════
📦 段2/3：[段描述]
  ...
═══════════════════════════════
📦 段3/3：[段描述]
  ...
═══════════════════════════════
```

---

## 输出顺序优先级

1. **先出资产Prompt** → 让用户先生成参考图
2. **再出分镜图Prompt** → 有了参考图后再生成分镜图
3. **最后出视频Prompt** → 所有参考图就绪后可执行

标记每个步骤的依赖关系：

```markdown
步骤依赖：
  步骤1 角色卡 ← 无依赖（可立即执行）
  步骤2 场景图 ← 无依赖（可并行执行）
  步骤3 分镜图 ← 依赖 步骤1+2（需等待角色和场景锚点）
  步骤4 视频Prompt ← 依赖 步骤1+2+3（需所有资产就绪）
```

---

## 费用预估

```markdown
💰 费用预估
  角色卡 ×[N]：[费用]
  场景图 ×[N]：[费用]
  分镜图 ×[N]：[费用]
  ─────────────
  资产总计：[总费用]
  视频生成：[费用/条]
  ─────────────
  项目总计：[总费用]
  预估总耗时：[时间]
```

---

## 联动

← 接收所有前序引擎的输出
← **读取 `state/variable-registry.md`**（project.title 等基础字段用于输出包标题和清单）
→ 整合为最终可执行包，输出给用户
→ 跨段衔接由 `shot-budget` 拆段时触发，本引擎只展示衔接信息
→ 如收到 auto-repair 的"需人工介入"标记 → 仍输出当前结果，但标注 ⚠ 需人工修复的项目
