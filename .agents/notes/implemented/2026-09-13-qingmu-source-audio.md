# Qingmu source sound in working cuts

Continuous film music cannot remove music already mixed into a generated clip. Working cuts therefore retain an explicit per-clip sourceAudioMode, with original audio as the default. Writer separates sound locally before the existing trim/mix pipeline; the cockpit and native director expose the same saved choice and runtime availability.

The optional Bandit environment stays outside the web process. Source media remain immutable, cached stems carry hashes, the render worker renews its existing lease and completed exports retain exact source-processing receipts. The model estimates sources and can damage details; local availability and successful export do not imply perceptual acceptance.

Verification covers saved selection and reopen, original-audio compatibility, absent runtime refusal before queueing, cache corruption, lease loss, real FFmpeg timing/mixing and a deployed browser export. The original real-film separation sample proves the external model runs; it is distinct from fixture tests and creative approval.
