# Native scene design handoff

A real new-project asset session called geometry and layout preview tools, then omitted space, sceneLayout and imageCamera from its final JSON. Its prose described a room but subsequent images had no saved composition. The final generic asset example omitted these scene-specific fields.

Include a scene example and instruct the director to carry the chosen preview layout and camera into the deliverable. Native adoption requires new scenes to declare shared layout prose and geometry/camera choices. Explicit null retains director control over text-only framing. A scene camera cannot refer to absent geometry. Missing fields leave the previous cards and database unchanged. Existing-scene supplements still merge omitted fields; manual legacy imports are unchanged. Reuse the shot-camera validator rather than admitting degenerate camera positions or unusable FOV.

Focused component checks cover rejected incomplete handoffs, geometry and explicit-null save/reload, and existing supplements; the native request snapshot records the actual output instructions. Deployed ordinary-browser adoption and image-preview checks establish that selected scene design reaches a request. They do not guarantee physical or artistic fidelity of the image model.
