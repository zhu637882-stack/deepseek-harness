# Complete starting images across Qingmu entry points

The first-frame compiler appended video-wide inherited context even after a director had written a complete starting image. Later action, lighting and sound could re-enter that still; a full room inventory could compete with its camera projection.

Reuse the asset-image `selfContainedImagePrompt` choice in saved director plans. New scene design, whole-scene reconciliation and the first-frame assistant instruct the director to author all visible detail, scale, era exceptions and lighting before enabling it. Ordinary shooting exposes the same choice, with exact-request draft recovery. Legacy plans retain their prior behavior. Current state, reference uses and visual style still apply; full video design and shared-source freshness remain intact. Writer validates the boolean, keeps it out of video prose, and uses the authored still without video-wide context. With a projected camera, the full prose inventory is not duplicated.

Verify the actual image payload plus unchanged video design, native save/read transport, explicit true/false UI recovery and deployed preview. These checks establish input fidelity, not physical or aesthetic compliance of generated pixels. No automatic migration or media adoption.
