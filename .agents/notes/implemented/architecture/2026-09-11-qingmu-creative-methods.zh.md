# Agent Note：青木原生会话中的创作方法

Status: implemented

[English](2026-09-11-qingmu-creative-methods.md) | 中文

## 问题

方法名称或局部模板不足以支撑导演流程。声音、表演、连续性和调度参考必须到达模型。上游对说话人数和运镜数量的假设也可能与项目剧本和导演决定冲突。

## 决策

青木预设使用原生技能发现和加载。cinematic-director 入口是青木自写的 Leos 六部门方法适配，包含全片理解与资产设计。Open Film Skills 提供完整编剧与摄影专项包，保留上游 Apache-2.0 许可；旧 DirectorSKILL 入口及 ai-visual-director 作为辅助资料。运行包不分发 Leos 源码。作用域内资料读取器校验清单路径与哈希，明确分页，分别记录上游和适配后来源。引入资料中的数量、格式和平台默认值仅供参考；剧本和导演设计决定如何执行。能力不足须明确执行替代方案，不能静默简化作品。

## 考虑过的替代方案

启动时注入整个方法库消耗上下文，却不能确保正确使用关联参考。再造技能注册表会重复 DSH 能力。只摘录部分句子会丢失方法及关联依赖。

## 影响

真实 Loader 和 Agent 循环测试验证技能目录、完整方法与参考进入后续模型请求，以及会话隔离。资料测试覆盖分页、来源完整性和拒绝路径。这些检查证明方法可读取，不代表真实模型已完成创作、设计已保存到项目、付费生成质量达标或用户认可。[来源清单](../../../../packages/experimental/qingmu-web/agent-presets/qingmu-director/skills/sources.json) 标明版本和排除的二进制案例媒体。

创作设定将当前执行方法与不可变创建历史分开。已审阅的 V2 前身按声明目标解析；V1 与未配置合同的项目在新创作中使用已安装原生方法，历史引用保留为辅助。画风选择器、编剧输入与参考视频导演读取同一份调和后的视觉设定。预设调和不处理剧本和导演自写正文；具体机位与光源由导演决定，视频预览同时解析单集风格覆盖和项目默认值。

场景引用工作台将当前镜头的准确导演时长带入未保存的视频草稿，替代固定八秒默认值；恢复时以已存草稿为准。分数秒或模型不支持的时长交给明确的参数校验，界面不静默改写导演设计。
