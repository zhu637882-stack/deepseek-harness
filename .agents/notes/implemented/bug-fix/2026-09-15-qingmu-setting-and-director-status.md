# Scene identity and director status

Status: implemented

English | [中文](2026-09-15-qingmu-setting-and-director-status.zh.md)

The imperial-study candidate retained room geometry but reduced its identity to a place name. Local prop repairs preserved the same plain furnishings. Shared asset authoring now translates the source location, occupant status and maintenance state into visible finishes and fixed decoration, distinguishes empty staging from empty architecture, and revisits the whole setting after local repairs. It does not impose palace decoration on unrelated projects or invent screenplay facts.

Scene planning also displayed an unselected shot-edit session alongside a running independent scene-authoring request. The details now identify their saved-shot scope, direct new projects to scene design and save, and remove static claims about film-wide prompt readiness and the obsolete product name. No session activation, generation or adoption behavior changes.

The existing asset request snapshot exercises the shared prompt; scene-planning tests cover creation, saved-shot binding and recovery. Actual generated scene identity remains a visual judgment. The production-design section of [Visual Media Analysis Expert](https://prompts.chat/prompts/cmmx3l25d001bil046oqvr24j_visual-media-analysis-expert-agent-role) informed only the link between visible materials and world context; its exhaustive checklists, metadata guesses and workflow rules were not adopted.

Voice directions must translate known age into audible character traits, and period props into material and usable construction. The media library labels full auditions and derived references separately. Shared actor-profile persistence is verified in the Writer API tests for creation, revision, omission and clearing without replacing enrolled audio.

The character, scene and prop methods now describe visible period construction, social identity and makeup, separating stable appearance from temporary story state. The immutable/mutable split and compact reference descriptions were independently adapted from [director-skills](https://github.com/0xhughs/director-skills), `character-location-prop-bible` and `cinematic-image-prompting`, commit `35ca0a2b4cd55b0668aa9ea0f40273324b774bb4` (MIT, Copyright 2026 0xhughs). The new production-design native skill bundles both pinned upstream entries, every linked reference/template and their MIT license. Its entry maps outputs to Qingmu fields, and the shared asset request loads it before authoring. No scripts or model clients were installed. prompts.chat remains a research source, not a live native-generation dependency.

The media preview now offers explicit manual deletion and restoration. Deleted media leaves the default reference catalog but its files, hashes and generation history remain recoverable. Writer binds each mutation to project ownership and the exact asset hash; selected or saved-design inputs remain protected, and deleted references cannot enter new image/video requests. Catalog changes refresh open reference pickers without replacing unsaved design text. The user alone chooses versions; no age-, quality- or date-based cleanup runs automatically.

Reusable prop methods and the shared authoring prompt also distinguish net weight, package dimensions and physical object dimensions, preserve source confidence and propagate consistent scale into hand-held and container relationships. Exact vendor measurements remain a source question, not an inference from image pixels.

Asset-library changes refresh generation progress as well as reference pickers. Deleted outputs leave the active run view; restoring an output reuses its original task without generating again. Unsaved design text remains intact.

Native authoring exposed a further mismatch: retained prop prompts omitted dimensions added to their identity text, and layout blocks used inconsistent supporting heights. Shared production-design read-back now distinguishes preserving media from updating its complete description; scene guidance checks ground and support surfaces, thin components, rotated extents and real openings before composition previews. Preview rendering is not physical validation.

Asset redesign separates confirmed identity from rejected proposals. Character guidance translates stated facial aesthetics, wardrobe changes and audible age into the actual generation fields; scene guidance establishes architectural structure before finishes and furniture. Output review remains necessary: successful requests do not establish visual or vocal acceptance. Derived voice excerpts remain accessible in a collapsed library section instead of competing with full auditions as additional voices.

Default asset authoring treats creative supplements as optional and derives appearance, construction, scale and audible identity from the brief and script. Native partial updates may retain an image prompt only when the subject identity is unchanged; voice-only revisions remain supported. Pending candidates retain their submitted supplement context so later edits cannot silently change its meaning. Empty-supplement request tests verify input routing, not media quality.

A live empty-supplement regression exposed rote reuse of saved designs. Production design and its consumer now distinguish design review from exporting JSON, with saved status never implying creative acceptance. Media quality remains unverified.

Asset authoring now sends an identity/reference index for saved working drafts and the complete text only for unsaved local edits. The director read tool explicitly separates persistence from creative acceptance. Cinematic image prompting leads design; continuity maintenance follows design rather than freezing unreviewed drafts. Empty-supplement live behavior remains subject to runtime verification.

The asset design primary action uses asset-design-from-script: the read tool returns identity/reference/spatial anchors without injecting old design prose. Revising the previous candidate and downstream authoring retain full saved reads. Neither action changes saved media or grants creative acceptance. This follows a failed live test where source labels alone still allowed unchanged character and room prompts.

Default asset authoring delegates detailed art methods to the existing asset skills instead of repeating them in the UI request. The production-design method turns provisional suggestions into deliberate casting, costume and architectural choices, then evaluates the concrete description without relying on role or place names. It returns a concise design outcome and consumer-ready fields; supplements remain optional. The existing save, reference and generation paths are unchanged. This removes 3,550 characters of repeated entry instructions; live creative results still require review.

The original brief is read through the scoped asset source tool rather than interpolated into the current user instruction. Its complete text remains available, including ages and story exceptions; effective style settings and explicit current supplements still travel with the request. This separates provisional source prose from the current design commission without another model call.

A live design session read the nested image-prompting skill but requested its lighting reference relative to the parent bundle, received a missing-resource error and still delivered its candidate. Resource pages now expose manifest-checked bundle-relative links and missing-path errors offer exact packaged matches without silently substituting content. Tests follow all six links in the shipped nested skill through the native reader. This repairs resource discovery, not creative quality or automatic acceptance.
